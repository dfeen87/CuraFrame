services:
  - type: web
    name: curaframe-console
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run apps/console_streamlit/app.py --server.port $PORT --server.address 0.0.0.0
    plan: free
